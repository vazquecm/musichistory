/// This module is filtering the songs by the choices the user inputs from the selection boxes //

define(["jquery", "firebase", "q", "script"], 
function ($, firebase, Q, script) {

 			
/// this click function allows the user to clear the options from the "add view" section
 	$("body").on("click","#filter", function() {
  console.log("lets filter some options");

	});




});
 